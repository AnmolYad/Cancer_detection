class Solution {
    public int search(int[] nums, int target) {
        int count=-1;
        for(int i=0;i<nums.length;i++){
            count++;
            if(target==nums[i]) return count;
        }
       return -1;
    }
}
